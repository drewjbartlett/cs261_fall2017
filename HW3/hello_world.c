#include <stdio.h>

/* Hello, World
    This is here just to make sure you can compile C
    If:
    gcc -std=c99 -o hello hello_world.c ; ./hello
    doesn't print:
    Hello, World!
    Then your environment might need some TLC
*/

int main()
{
   // printf() displays the string inside quotation
    printf("Hello, World!\n");
    return 0;
}

